apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sonarr-config
  namespace: jellyfin
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: truenas-nfs-csi
  resources:
    requests:
      storage: 5Gi
