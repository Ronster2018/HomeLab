apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: radarr-config
  namespace: jellyfin
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: truenas-nfs-csi
  resources:
    requests:
      storage: 5Gi
