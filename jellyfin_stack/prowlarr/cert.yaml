apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: prowlarr
  namespace: jellyfin
spec:
  # Secret names are always required.
  secretName: prowlarr-tls
  issuerRef:
    name: cloudflare-dns-issuer
    kind: ClusterIssuer
  dnsNames:
  - prowlarr.home.sparhomelab.dev
